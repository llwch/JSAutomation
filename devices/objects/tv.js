/*MainDevice = require('../mainDevice');

class TV extends MainDevice {
    mode (room, power, weight, numberofChannels) {
        super(room, room, power, weight);
        this.numberofChannels = numberofChannels;
    }
}

module.exports = TV;
*/